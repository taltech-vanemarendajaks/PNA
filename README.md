# PNA
Phone Number Analyser

For local backend setup, see `backend/README.md`.

For the full local stack with Docker, run:

```bash
docker compose up
```

That starts:

- PostgreSQL on `localhost:5432`
- backend on `http://localhost:8080`
- frontend on `http://localhost:5173`
